#include <iostream>
#include <cassert>

namespace lcl::testing_detail
{
    void detail_assert_that(const bool assertion_is_valid, const int line, const int filename)
    {
        if (assertion_is_valid)
        {
            std::cout << 
        }
    }
}